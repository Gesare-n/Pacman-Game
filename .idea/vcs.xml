<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <!-- Integrates the project with version control (Git in this case) -->
  <component name="VcsDirectoryMappings">
    <mapping directory="$PROJECT_DIR$" vcs="Git" />
  </component>
</project>
